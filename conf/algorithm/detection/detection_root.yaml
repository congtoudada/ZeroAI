INCLUDE:
  - conf/global/log.yaml
  - conf/global/output.yaml
log:
  enable: True
stream:
  output_dir: output/detection  # 输出目录
  save_video:
    enable: False  # 是否保存视频
  draw_vis:
    enable: False  # 是否可视化
