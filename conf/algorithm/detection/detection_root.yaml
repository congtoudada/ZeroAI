INCLUDE:
  - conf/global/log.yaml
  - conf/global/output.yaml
log:
  enable: False
stream:
  output_dir: output/detection  # 输出目录
  save_video:
    enable: True  # 是否保存视频
  draw_vis:
    enable: True  # 是否可视化
