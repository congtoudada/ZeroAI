INCLUDE:
  - conf/pro/algorithm/yolox/yolox_root.yaml
  - conf/pro/algorithm/yolox/labels/count_labels.yaml

input_ports:  # 输入端口
  - camera1

output_ports:  # 输出端口
  - yolox-count1

stream:
  save_video:
    filename: count

yolox:
  args:
    expn: count  # 实验名称
    exp_file: conf/pro/algorithm/yolox/exps/yolox_s_count.py  # 通过.py选择模型
    ckpt: pretrained/yolox/count/yolox_s_count.pth # 推理用模型权重文件
    conf: 0.55 # test conf
