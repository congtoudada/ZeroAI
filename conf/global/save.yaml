save_video:
  width: 640
  height: 480
  fps: 24

# 是否存储视频（可选，记得在visual.yaml中开启可视化）
yolox:
  save_video: True
  output_dir: output/detection/yolox
bytetrack:
  save_video: False
  output_dir: output/mot/bytetrack
count:
  save_video: False
  output_dir: output/business/count
