INCLUDE:
  - conf/dev/global/log.yaml
  - conf/dev/global/output.yaml
log:
  enable: False
stream:
  output_dir: output/business/phone  # 输出目录
  save_video:
    enable: False  # 是否保存视频
  draw_vis:
    enable: True  # 是否可视化

