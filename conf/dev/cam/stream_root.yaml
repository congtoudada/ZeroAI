INCLUDE:
  - conf/dev/global/log.yaml
stream:
  # 是否以delay模式运行（开启后取本地流会引入延迟，避免大量丢帧）
  delay: False
  # delay加速
  delay_speed: 1
  # 回放视频帧缓存上限 (决定了抓拍视频从之前帧开始的程度，该值越大显存占用越大）
  frame_history_capacity: 10
  # 手动取帧数（越大性能越差，准确率高） eg.2: 每2帧里面取1帧
  drop_interval: 2
  # 画面缩减倍数
  scale: 2
  # 算法update帧率, <=0 表示无限制
  update_fps: 60
