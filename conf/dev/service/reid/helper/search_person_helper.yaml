reid:
  face_gallery_dir: output/service/clip_reid/face_gallery  # 由人脸识别捕捉的带id的人像gallery
  min_send_interval: 30  # 最快每多少帧发送一次人脸请求（小于0为不限）
  max_retry: 5  # 单人最大重试次数
  quest_method:  # 支持的请求方式 1:存图请求(可不配置) 2:Reid请求 3:找人请求
    - 3